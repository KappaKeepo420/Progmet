#include <iostream> 
#include <string>
#include <cstdlib>
#include <fstream>
#include <climits>

using namespace std;

int main() {
	char optie;
	switch(optie) {
		case 'C' :
		case 'R' :
		case 'P' :
		case 'S' :
		default :
			cout << "Voer aub een geldige optie in.\n"
	}
}